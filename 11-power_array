#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int n;
    int array[n];
    int pwr;
    pwr = n * 2;

    printf("Enter the value of n: ");
    scanf("%i", &n);

    printf("power of array[n]: %i\n", pwr = (n * 2));
    scanf("%i", &pwr);

    return 0;
}
