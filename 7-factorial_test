#include <stdio.h>
#include <stdlib.h>

unsigned int factorial(unsigned int n)
{
    if (n <= 1)
    {
        return (1);
    }
    return n * factorial(n - 1);
}

int main()
{
    printf("factorial of n is: %d\n", factorial(4));
    return 0;
}
